{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
  "exec": "ts-node --require tsconfig-paths/register src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
